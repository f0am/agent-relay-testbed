{
  // Demo config for the agent sandbox's devcontainer support.
  // Honored: postCreateCommand, containerEnv, customizations.agentSandbox.agent.
  "postCreateCommand": "npm install --no-save left-pad",
  "containerEnv": { "NODE_ENV": "development" }
}
